# WorkBuddy 提速包

打开本文件夹会加载 `.codebuddy/` 下的瘦配置与 rules。

若用户要求「按链接安装到本机全局」，先读根目录 `给WorkBuddy.md`，运行 `scripts/install.py`：只合并 `hard-settings.json`，并复制 `rules/fast-default.md` 到用户级 rules 目录。不要覆盖 `claw` / `sandbox`，不要改 `MEMORY.md`。
